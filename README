Run `rake` to set up a remote server that:

- Listens on port 514 for input from Heroku logplex (http://devcenter.heroku.com/articles/logging)
- Saves heroku[nginx] lines to /var/log/heroku.log
- Rotates heroku.log every hour, compresses the rotated log and uploads it to s3